# Prompt Craft

A React + Vite project for building Prompt Craft.

## Tech Stack

- React 18
- Vite 5

## Getting Started

### Prerequisites

- Node.js 18+ (recommended)
- npm

### Install

```bash
npm install
```

### Run in Development

```bash
npm run dev
```

Vite will print a local URL (usually `http://localhost:5173`).

## Available Scripts

- `npm run dev` - Start the development server
- `npm run build` - Build production assets into `dist/`
- `npm run preview` - Preview the production build locally

## Project Structure

```text
.
├── index.html
├── package.json
├── vite.config.js
├── public/
│   └── images/
└── src/
    ├── App.jsx
    ├── index.css
    └── main.jsx
```

## Environment Variables

This project includes `.env` and `.env.example`.

1. Copy `.env.example` to `.env` if needed.
2. Add your values in `.env`.
3. Restart the dev server after changing environment variables.

## Build for Production

```bash
npm run build
npm run preview
```

## License

Private project.
